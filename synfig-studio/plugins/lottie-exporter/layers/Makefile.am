# Id $

PLUGIN_NAME = layers

EXTRA_FILES = \
			  image.py \
              shape.py \
			  solid.py \
			  shape_solid.py

plugindir = ${datadir}/synfig/plugins/lottie-exporter/$(PLUGIN_NAME)
plugin_DATA = \
                $(EXTRA_FILES)

EXTRA_DIST = \
             $(EXTRA_FILES)

MAINTAINERCLEANFILES = Makefile.in

