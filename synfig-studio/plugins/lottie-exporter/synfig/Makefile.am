# Id $

PLUGIN_NAME = synfig

EXTRA_FILES = \
			  animation.py \
			  group.py \
			  rectangle.py

plugindir = ${datadir}/synfig/plugins/lottie-exporter/$(PLUGIN_NAME)
plugin_DATA = \
                $(EXTRA_FILES)

EXTRA_DIST = \
             $(EXTRA_FILES)

MAINTAINERCLEANFILES = Makefile.in

